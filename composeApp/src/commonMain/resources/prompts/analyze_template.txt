Please strictly analyze the provided UI images. You MUST recognize and break down the ENTIRE UI displayed in the image into granular, independent functional modules/blocks following the specification below.

Generate a comprehensive JSON structure representing this complete UI template breakdown.

The JSON output must strictly map to the following Kotlin classes (do NOT wrap in markdown blocks, return pure JSON):
ProjectState(projectId: String, globalTheme: String, pages: List<UIPage>)
UIPage(id: String, nameStr: String, width: Float, height: Float, blocks: List<UIBlock>)
UIBlock(id: String, type: UIBlockType, bounds: SerialRect(left, top, right, bottom), userPromptEn: String, userPromptZh: String, children: List<UIBlock>)

All component classifications, coordinate hierarchies, REEL/SYMBOL decoupling rules, and prompt fidelity standards are defined in the specification text below. You MUST strictly adhere to every directive therein.
