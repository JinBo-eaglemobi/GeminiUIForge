Please strictly analyze these images. You MUST recognize and break down the ENTIRE UI displayed in the image into distinct, granular functional modules/blocks. Consider how these components can be logically segmented into independent assets for automated export as transparent PNGs via scripts. Do not just focus on the main area; analyze headers, footers, panels, texts, buttons, backgrounds, icons, and decorations.

Generate a comprehensive JSON structure representing this complete UI template breakdown.

CRITICAL REQUIREMENT: 
1. EVERY functional layer, background, content panel, or interactive element displayed in the image MUST have a corresponding distinct template (UIBlock) with accurate bounding coordinates.
2. The `width` and `height` in `UIPage` MUST represent the total canvas size (resolution) of the analyzed image.
3. ALL `UIBlock` coordinates (left, top, right, bottom) MUST be strictly within the bounds of [0, width] for X-axis and [0, height] for Y-axis.

The JSON must strictly map to the following Kotlin classes (do NOT wrap in markdown blocks, return pure JSON):
ProjectState(projectId: String, globalTheme: String, pages: List<UIPage>)
UIPage(id: String, nameStr: String, width: Float, height: Float, blocks: List<UIBlock>)
UIBlock(id: String, type: UIBlockType, bounds: SerialRect(left, top, right, bottom), userPrompt: String)

UIBlockType MUST be one of the following exact strings:
- REEL (Slot machine reel area)
- SPIN_BUTTON (Main spin button)
- WIN_DISPLAY (Win score display box)
- BACKGROUND (Full screen game background)
- SYMBOL (Individual slot machine symbol)
- BUTTON (Generic interactive button)
- PANEL (Content panel or container box)
- HEADER (Top header bar, title area)
- FOOTER (Bottom footer bar, navigation)
- TEXT_AREA (Text display area)
- ICON (Small icon or badge)
- DECORATION (Ornamental or decorative UI element)

ASSET GENERATION & PROMPT REQUIREMENTS:
1. For EACH `UIBlock`, the `userPrompt` field MUST contain a detailed and high-quality prompt describing that specific component.
2. The `userPrompt` SHOULD instruct the generation model to strictly follow the visual style, shape, and colors of the UI components seen in the provided reference image.
3. For all interactive or standalone components (such as BUTTON, ICON, SYMBOL, PANEL, etc.), the `userPrompt` MUST explicitly require a "SOLID CONTRAST COLOR BACKGROUND" (e.g., solid neon green or magenta) that does not exist in the UI element itself.
4. Explain in the `userPrompt` that this background is intended for automated script-based background removal to create a transparent PNG with an alpha channel.
5. The `userPrompt` should be a standalone prompt that can be directly used by an image generator to produce a high-fidelity recreation of the specific UI module.