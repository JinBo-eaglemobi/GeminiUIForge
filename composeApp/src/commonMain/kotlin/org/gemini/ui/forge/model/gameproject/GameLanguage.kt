package org.gemini.ui.forge.model.gameproject

/**
 * 游戏项目使用的开发语言枚举。
 * 当前仅支持 TypeScript，但通过枚举结构预留后续扩展（如 JavaScript、Cocos Creator 等）。
 */
enum class GameLanguage {
    /** TypeScript 语言（当前唯一支持项） */
    TYPESCRIPT
}
